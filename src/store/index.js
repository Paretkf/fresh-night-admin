import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const actions = {
}

const mutations = {
}

const getters = {
}

const state = {
}

const modules = {
}

export default new Vuex.Store({
  state,
  actions,
  mutations,
  getters,
  modules
})
